version: 2
updates:
  # Pinned action SHAs go stale silently; Dependabot bumps them with the
  # version comment kept in sync.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
    labels: [dependencies]
