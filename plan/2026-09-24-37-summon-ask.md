---
status: draft
issue: 37
spec: spec/2026-09-24-37-summon-ask.md
---

# Plan: Open Nixi with a question already asked

## Approved decisions (carried over from the spec)

- **`Ask.qml` `askFromSummon(conversation, payload)`:** trim `payload.prompt`,
  and ignore it if empty. If `conversation.waiting` or the text starts with
  `/`, call `conversation.setPrompt(text)` (fill only). Otherwise call
  `conversation.askQuestion(text)`.
  - A hand-off never runs `/guide`, `/mechanic`, `/tour` or `/learn`, and
    never steers or interrupts a running turn.
- **Callers:**
  - **New card:** in `createConversation`, next to `tour` and `learn`,
    `action === "ask"` calls `askFromSummon`.
  - **Open, unpinned card:** in `open()`, parse the payload before the early
    return, and for `action === "ask"` call `askFromSummon(activeOverlay, …)`.
    Other payloads are unchanged.
  - **Pinned cards:** they are never the target; a new overlay card asks.
- **`Conversation.qml` `setPrompt(text)`:** set the text, focus, and put the
  cursor at the end.
- **`bin/nixi --ask <prompt>`:**
  - A missing or empty argument exits 64.
  - `card_ready || exit 1`.
  - Escape in bash (`\`, `"`, newline, tab), then `exec omarchy-shell shell summon "$PLUGIN" '{"action":"ask","prompt":…}'`.
  - Update the header args line and the usage line.
  - No jq.

## Steps (one implementer: the lead; about 20 lines)

1. **`Conversation.qml`:** add `setPrompt(text)` next to `askQuestion` (~1177).
   → Verify: qmllint shows no new warning.
2. **`Ask.qml`:** add `askFromSummon`, the `createConversation` branch and the
   `open()` handling.
   → Verify: qmllint; `tour` and `learn` paths unchanged in the diff.
3. **`bin/nixi`:** add the `--ask` case, header and usage.
   → Verify: `shellcheck -S warning bin/nixi`.
4. **`tools/test_nixi.py` `test_nixi_launcher`:**
   - with the card enabled: `--ask 'say "hi"<newline>now<tab>x\y'` → exit 0,
     exactly one `shell summon <card>` logged, and its JSON argument parses
     with `json.loads` to the same prompt
   - with the card off: `--ask x` → exit 1, no summon
   - `--ask` alone → exit 64

   → Verify: `python3 tools/test_nixi.py` passes, and `nix flake check` passes.
5. **`docs/testing.md`:** add the manual steps from spec Verification 3 to the
   checklist.
6. **Razer manual check.** Announce on the agent bus first.
   - razer's plugin dir `~/.config/omarchy/plugins/io.github.olafkfreund.nixi`
     is a Home Manager symlink into `/nix/store`.
   - Record the symlink target, replace it with a copy of the branch's plugin
     files, and restart the shell.
   - Run the branch's `bin/nixi --ask` through the desktop env (`~/dev/rz.sh`).
   - Check each case in spec Verification 3:
     - new card, idle card, busy card
     - `/mechanic` only prefills, with the trust level unchanged
     - `tour` still works
   - **Restore the original symlink exactly** (`ln -sfn <recorded target>`),
     then restart the shell.
7. **PR:** push `feat/37-summon-ask` and open a PR against `master` that links
   the three artifacts. Merge it (squash, the repo's convention) when CI is
   green and the user agrees.

## Tests

- `python3 tools/test_nixi.py`
- `shellcheck -S warning bin/nixi`
- `nix flake check` (the repo's CI job)
- the manual razer checks in step 6

## Rollback

- Revert the squash commit. The `tour`, `learn` and toggle paths are
  unchanged by design.
- Razer: the symlink is restored in step 6, and nothing persists.
